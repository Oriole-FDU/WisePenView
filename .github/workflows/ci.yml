name: Continuous Integration

on:
  pull_request:
    branches: [ "main" ]

jobs:
  verify-and-build:
    name: Code Quality & Build Gate
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 

      # 【核心新增】1. 在 setup-node 之前，先安装 pnpm
      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.33.0 # 与 packageManager 保持一致，避免 lockfile / store 版本差异

      # 2. 设置 Node.js，并将缓存机制切换为 pnpm
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm' 

      # 【核心修改】3. 严格依赖安装
      # --frozen-lockfile 等价于 npm ci，它严格读取 pnpm-lock.yaml，绝不自动更新依赖
      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      # 4. Commit 规范检查 (此 Action 内部兼容 pnpm)
      - name: Validate PR Commits
        uses: wagoid/commitlint-github-action@v6

      # 【核心修改】5. 使用 pnpm 执行脚本
      - name: Lint Code
        run: pnpm run lint

      # 6. 编译检查：package.json 的 build 已包含 tsc -b 和 vite build
      - name: Build Project
        run: pnpm run build
